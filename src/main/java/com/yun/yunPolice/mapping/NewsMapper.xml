<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yun.yunPolice.dao.INewsDao" >
    <resultMap id="BaseResultMap" type="com.yun.yunPolice.pojo.News" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="title" property="title" jdbcType="VARCHAR" />
        <result column="author" property="author" jdbcType="VARCHAR" />
        <result column="source" property="source" jdbcType="VARCHAR" />
        <result column="content" property="content" jdbcType="VARCHAR" />
        <result column="imgs" property="imgs" jdbcType="DATE" />
        <result column="times" property="times"/>
    </resultMap>

    <sql id="Base_Column_List" >
        id, title, author, source, content, imgs, times
    </sql>

    <select id="getList" parameterType="HashMap" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM news ORDER BY  #{sortname,jdbcType=VARCHAR}  #{sortorder,jdbcType=VARCHAR} LIMIT #{page,jdbcType=INTEGER}, #{pagesize,jdbcType=INTEGER};
    </select>

    <select id="totalList" resultType="Integer">
        SELECT count(id) FROM news;
    </select>

    <select id="get" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM news WHERE id = #{id,jdbcType=INTEGER};
    </select>

    <delete id="del" parameterType="java.lang.Integer">
        DELETE FROM news
        WHERE id = #{id,jdbcType=INTEGER}
    </delete>

    <insert id="save" parameterType="com.yun.yunPolice.pojo.News">
        insert into news (title, author, source, content, imgs, times
        )
        values (#{title,jdbcType=VARCHAR}, #{author,jdbcType=VARCHAR}, #{source,jdbcType=VARCHAR}, #{content,jdbcType=VARCHAR}, #{imgs,jdbcType=VARCHAR}, #{times,jdbcType=VARCHAR})
    </insert>

    <update id="update" parameterType="com.yun.yunPolice.pojo.News" >
        UPDATE news
        <set >
            <if test="title != null" >
              title = #{title,jdbcType=VARCHAR},
            </if>
            <if test="author != null" >
                author = #{author,jdbcType=VARCHAR},
            </if>
            <if test="source != null" >
                source = #{source,jdbcType=VARCHAR},
            </if>
            <if test="content != null" >
                content = #{content,jdbcType=VARCHAR},
            </if>
            <if test="imgs != null" >
                imgs = #{imgs,jdbcType=VARCHAR},
            </if>
        </set>
        WHERE id = #{id,jdbcType=INTEGER}
    </update>
</mapper>